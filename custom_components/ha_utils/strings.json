{
  "config": {
    "step": {
      "user": {
        "title": "Home Assistant Utils",
        "description": "Register the Developer Tools action used to set the default Home Assistant theme font scale."
      }
    },
    "abort": {
      "single_instance_allowed": "Home Assistant Utils is already configured."
    }
  }
}
